<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youran.generate.dao.MetaProjectDAO">

    <select id="findById" resultType="MetaProjectPO">
        select * from meta_project
        where delSign=0
        and projectId = #{projectId,jdbcType=INTEGER}
    </select>

    <select id="exist" resultType="boolean">
        select count(*) from meta_project
        where delSign=0
        and projectId = #{projectId,jdbcType=INTEGER}
    </select>

    <insert id="_save" useGeneratedKeys="true" keyProperty="projectId" parameterType="MetaProjectPO">
        insert into meta_project (
          projectId, packageName, projectName, groupId,
          author,remote,remoteUrl,username,
          password,lastHistoryId,projectVersion,
          createDate, createBy,
          operateDate, operateBy, delSign,
          version)
        values (#{projectId,jdbcType=INTEGER}, #{packageName,jdbcType=VARCHAR}, #{projectName,jdbcType=VARCHAR}, #{groupId,jdbcType=VARCHAR},
          #{author,jdbcType=VARCHAR},#{remote,jdbcType=INTEGER},#{remoteUrl,jdbcType=VARCHAR},#{username,jdbcType=VARCHAR},
          #{password,jdbcType=VARCHAR},#{lastHistoryId,jdbcType=INTEGER},#{projectVersion,jdbcType=INTEGER},
          #{createDate,jdbcType=TIMESTAMP}, #{createBy,jdbcType=VARCHAR},
          #{operateDate,jdbcType=TIMESTAMP}, #{operateBy,jdbcType=VARCHAR}, #{delSign,jdbcType=SMALLINT},
          #{version,jdbcType=INTEGER})
    </insert>

    <update id="_update" parameterType="MetaProjectPO">
        update meta_project
        set
            packageName = #{packageName,jdbcType=VARCHAR},
            projectName = #{projectName,jdbcType=VARCHAR},
            groupId = #{groupId,jdbcType=VARCHAR},
            author = #{author,jdbcType=VARCHAR},
            remote = #{remote,jdbcType=INTEGER},
            remoteUrl = #{remoteUrl,jdbcType=VARCHAR},
            username = #{username,jdbcType=VARCHAR},
            password = #{password,jdbcType=VARCHAR},
            lastHistoryId = #{lastHistoryId,jdbcType=INTEGER},
            projectVersion = #{projectVersion,jdbcType=INTEGER},
            operateDate = #{operateDate,jdbcType=TIMESTAMP},
            operateBy = #{operateBy,jdbcType=VARCHAR},
            version = #{version,jdbcType=INTEGER}
        where projectId = #{projectId,jdbcType=INTEGER}
        and version=#{version,jdbcType=INTEGER}
        and delSign=0
    </update>

    <delete id="delete">
        update meta_project set
        delSign=1
        where projectId = #{projectId,jdbcType=INTEGER}
        and delSign=0
    </delete>

    <select id="findListByQuery" parameterType="MetaProjectQO" resultType="MetaProjectListVO">
        select * from meta_project t
        where t.delSign=0
        order by t.createDate
    </select>
</mapper>
