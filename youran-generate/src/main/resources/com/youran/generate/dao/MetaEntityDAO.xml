<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youran.generate.dao.MetaEntityDAO">

    <select id="findById" resultType="MetaEntityPO">
        select * from meta_entity
        where del_sign=0
        and entity_id = #{entityId,jdbcType=INTEGER}
    </select>

    <select id="exist" resultType="boolean">
        select count(*) from meta_entity
        where del_sign=0
        and entity_id = #{entityId,jdbcType=INTEGER}
    </select>

    <insert id="_save" useGeneratedKeys="true" keyProperty="entityId" parameterType="MetaEntityPO">
        insert into meta_entity (
          entity_id, project_id, `schema_name`,
          class_name, `table_name`, title, `desc`,
          common_call,page_sign, create_date, create_by,
          operate_date, operate_by, del_sign,
          version)
        values (#{entityId,jdbcType=INTEGER}, #{projectId,jdbcType=INTEGER}, #{schemaName,jdbcType=VARCHAR},
          #{className,jdbcType=VARCHAR}, #{tableName,jdbcType=VARCHAR}, #{title,jdbcType=VARCHAR},
          #{desc,jdbcType=VARCHAR},
          #{commonCall,jdbcType=SMALLINT},#{pageSign,jdbcType=SMALLINT}, #{createDate,jdbcType=TIMESTAMP}, #{createBy,jdbcType=VARCHAR},
          #{operateDate,jdbcType=TIMESTAMP}, #{operateBy,jdbcType=VARCHAR}, #{delSign,jdbcType=SMALLINT},
          #{version,jdbcType=INTEGER})
    </insert>

    <update id="_update" parameterType="MetaEntityPO">
        update meta_entity
        set project_id = #{projectId,jdbcType=INTEGER},
          `schema_name` = #{schemaName,jdbcType=VARCHAR},
          class_name = #{className,jdbcType=VARCHAR},
          `table_name` = #{tableName,jdbcType=VARCHAR},
          title = #{title,jdbcType=VARCHAR},
          `desc` = #{desc,jdbcType=VARCHAR},
          common_call = #{commonCall,jdbcType=SMALLINT},
          page_sign = #{pageSign,jdbcType=SMALLINT},
          operate_date = #{operateDate,jdbcType=TIMESTAMP},
          operate_by = #{operateBy,jdbcType=VARCHAR},
          version = #{version,jdbcType=INTEGER}
        where entity_id = #{entityId,jdbcType=INTEGER}
        and version=#{version,jdbcType=INTEGER}
        and del_sign=0
    </update>

    <delete id="delete">
        update meta_entity set
        del_sign=1
        where entity_id = #{entityId,jdbcType=INTEGER}
        and del_sign=0
    </delete>

    <select id="findCountByQuery" parameterType="MetaEntityQO" resultType="int">
        select count(1) from meta_entity t
        where t.del_sign=0
        and t.project_id = #{projectId,jdbcType=INTEGER}
    </select>

    <select id="findListByQuery" parameterType="MetaEntityQO" resultType="MetaEntityListVO">
        select * from meta_entity t
        where t.del_sign=0
        and t.project_id = #{projectId,jdbcType=INTEGER}
        order by t.create_date
        limit #{startIndex},#{pageSize}
    </select>

    <select id="findIdsByProject" resultType="int">
        select entity_id from meta_entity
        where del_sign=0
        and project_id = #{projectId,jdbcType=INTEGER}
        order by entity_id
    </select>
</mapper>
